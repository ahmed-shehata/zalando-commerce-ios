# Tests

github "bonkey/Nimble" "master" # fork with a fix for swiftlint build failure, to be changed back when it's merged in main repo

# AtlasMockAPI

github "bonkey/swifter" "stable" # fork with compilation fix (b800641)

# AtlasDemo

github "SwiftyJSON/SwiftyJSON"
github "bignerdranch/Freddy"
github "kean/Nuke"
github "daltoniam/SwiftHTTP"


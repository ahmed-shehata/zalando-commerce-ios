codecov:
  branch: master

coverage:
  precision: 2
  round: down
  range: 50...80

  ignore:
    - .*/SwiftyJSON.*
    - AtlasUI/AtlasUI/Utils/Reachability.swift

  status:
    project:
      default:
        target: 75
        threshold: 1

